@using Microsoft.Ajax.Utilities
@model IEnumerable<RentABike.Models.Customer>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Customer's</h2>

@if (!Model.Any())
{
    <p>Currently We Don't Hvae Customer's</p>
}
else
{

    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th>Name</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var customers in Model)
            {
                <tr>
                    <td>
                        @Html.ActionLink(@customers.Name, "Details", "Customers", new {Id = customers.Id}, null)
                    </td>
                </tr>
            }
        </tbody>
    </table>
}